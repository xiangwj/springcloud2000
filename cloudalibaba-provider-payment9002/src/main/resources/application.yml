server:
  port: 9002
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: centos.cnx.com:8848 
management:
  endpoints:
    web:
      exposure:
        include: '*'        